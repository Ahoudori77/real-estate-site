<section
  class="section-with-bg hero-section"
  style="
    --section-bg-image: url('/images/sections/hero-kyoto-north.jpg');
    --section-bg-opacity: 1;
    --section-overlay-left: 0.5;
    --section-overlay-center: 0.25;
    --section-overlay-right: 0;
  "
>
  <div class="container">
    <div class="hero-content">
      <p class="hero-eyebrow">REAL ESTATE SALES</p>

      <h1 class="hero-title">
        京都北部で、土地と住まいに向き合う。
      </h1>

      <p class="hero-lead">
        京丹後市・宮津市・舞鶴市・福知山市など、京都北部と周辺エリアで、
        土地・戸建て探しを丁寧にお手伝いします。
      </p>

      <form class="hero-search-card" action="/properties" method="get">
        <input
          type="text"
          name="city"
          placeholder="地域名で検索"
          aria-label="地域名で検索"
        />

        <select name="type" aria-label="物件種別">
          <option value="">土地・戸建て</option>
          <option value="land">土地</option>
          <option value="house">戸建て</option>
        </select>

        <button type="submit">
          検索する
        </button>
      </form>
    </div>
  </div>
</section>

<style>
  .hero-section {
    min-height: 560px;
    padding: 96px 0 104px;
    display: flex;
    align-items: center;
  }

  .hero-content {
    max-width: 980px;
  }

  .hero-eyebrow {
    margin: 0 0 18px;
    color: var(--color-accent);
    font-size: 14px;
    font-weight: 900;
    letter-spacing: 0.12em;
  }

  .hero-title {
    margin: 0;
    color: var(--color-text);
    font-size: clamp(40px, 4.3vw, 58px);
    line-height: 1.18;
    font-weight: 900;
    letter-spacing: -0.04em;
    text-wrap: balance;
  }

.hero-lead {
  max-width: 760px;
  width: fit-content;
  margin: 24px 0 0;
  padding: 10px 10px;
  border-radius: 14px;
  background: rgba(255, 250, 244, 0.205);
  color: var(--color-text);
  font-size: 17px;
  line-height: 2;
  font-weight: 700;
  box-shadow: 0 8px 20px rgba(47, 39, 36, 0.08);
  backdrop-filter: blur(6px);
}

  .hero-search-card {
    display: grid;
    grid-template-columns: minmax(220px, 1fr) 180px 170px;
    gap: 14px;
    max-width: 760px;
    margin-top: 34px;
    padding: 18px;
    border: 1px solid var(--color-border);
    border-radius: 14px;
    background: rgba(255, 255, 255, 0.92);
    box-shadow: 0 18px 40px rgba(47, 39, 36, 0.12);
    backdrop-filter: blur(8px);
  }

  .hero-search-card input,
  .hero-search-card select {
    width: 100%;
    min-height: 52px;
    border: 1px solid var(--color-border);
    border-radius: 14px;
    background: #fff;
    color: var(--color-text);
    padding: 0 16px;
    font: inherit;
    font-weight: 700;
    outline: none;
  }

  .hero-search-card input::placeholder {
    color: #9b8b84;
  }

  .hero-search-card input:focus,
  .hero-search-card select:focus {
    border-color: var(--color-accent);
    box-shadow: 0 0 0 4px var(--color-accent-soft);
  }

  .hero-search-card button {
    min-height: 52px;
    border: 1px solid var(--color-accent);
    border-radius: 999px;
    background: var(--color-accent);
    color: #fff;
    font: inherit;
    font-weight: 900;
    cursor: pointer;
    box-shadow: var(--shadow-accent);
    transition:
      background 0.2s ease,
      border-color 0.2s ease,
      transform 0.2s ease,
      box-shadow 0.2s ease;
  }

  .hero-search-card button:hover {
    background: var(--color-accent-dark);
    border-color: var(--color-accent-dark);
    transform: translateY(-1px);
    box-shadow: 0 14px 26px rgba(201, 54, 34, 0.22);
  }

  @media (max-width: 900px) {
    .hero-section {
      min-height: auto;
      padding: 72px 0 80px;
    }

    .hero-content {
      max-width: 100%;
    }

    .hero-search-card {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 640px) {
    .hero-section {
      padding: 56px 0 64px;
    }

    .hero-title {
      font-size: clamp(34px, 10vw, 46px);
      line-height: 1.22;
    }

    .hero-lead {
      font-size: 15px;
      line-height: 1.9;
    }

    .hero-search-card {
      margin-top: 28px;
      padding: 14px;
      border-radius: 20px;
    }
  }
</style>